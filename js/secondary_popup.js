// get current date
// get date stored with website

// if current date and queried date match
  // decrement LimitMe
  // if limit is zero
    // show overlay with message
  // else they have more views
    // show notification with how many views left
// if dates don't match match
  // set date to today
  // reset limit
